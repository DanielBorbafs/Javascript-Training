// Solicitar ao usuário uma entrada diferente de 0
let numero = 0;
while (numero <= 0) {
    numero = parseInt(prompt("Digite um número positivo diferente de zero:"));
}
alert("Você digitou: " + numero);

